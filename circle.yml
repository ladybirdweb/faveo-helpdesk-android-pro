#
# Build configuration for Circle CI
#

general:
    artifacts:
        - /home/ubuntu/faveo-helpdesk-android-pro/app/build/outputs/apk/

machine:
    environment:
        ANDROID_HOME: C:\Users\Lenovo\AppData\Local\Android

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-25.0.2,android-25,extra-google-m2repository,extra-google-google_play_services,extra-android-support
        - ANDROID_HOME=C:\Users\Lenovo\AppData\Local\Android

test:
    override:
        - (./gradlew assemble):
            timeout: 360